import React from 'react';
import { Link } from 'react-router';


export default class QuizShowAnswer extends React.Component {
	render() {/*
		var output="";
		console.log('answer ', this.props.answer);
		for(var user in this.props.answer){
			if (this.props.answer.hasOwnProperty(user)) {
				output += {user} + " : " + {this.props.answer[user]} + "<br/>";
			}
		}
		return(
			<div>
				{output}
			</div>
		)
	}*/
}
